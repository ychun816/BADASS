# router_yilin (second router) — runtime notes and FRR skeleton (no IPs configured)
# Use the same workflow as router1: assign IPs per-topology, start FRR, then configure protocols.

# Example runtime steps (replace IPs with topology-specific values):
#   ip addr add 192.0.2.3/24 dev eth0
#   ip link set eth0 up
#   ip addr add 10.0.0.2/24 dev eth1
#   ip link set eth1 up
#   /usr/lib/frr/docker-start

# Example FRR snippets (run inside vtysh)
# hostname router_yilin
#!
# router ospf
#  passive-interface default
#  no passive-interface eth0
#!
# router isis
#!
# router bgp 65000
#  bgp router-id 1.1.1.2
#  no bgp default ipv4-unicast
#  neighbor 1.1.1.254 remote-as 65000
#  address-family l2vpn evpn
#    neighbor 1.1.1.254 activate
#  exit-address-family
